/**
 * Loading Logo — REMOVED.
 * Logo canvas has been removed; "GLO KARTS" title text is shown directly in HTML.
 * This stub exists so any remaining imports don't break.
 */

// Record splash start for the 3-second minimum hold
window.__splashStart = performance.now();
window.__loadingLogo = { dispose() {} };

class LoadingLogo { constructor() {} dispose() {} }
export default LoadingLogo;
